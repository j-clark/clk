<% @posts.each_cons(2) do |first, second| %>
  <div class="row">
    <div class="span8">
      <span><%= first.title %></span><br>
      <span>by: <%= link_to first.user.username, users_url(first.user.id) %></span>
      <p><%= first.body %></p>
    </div>
    <div class="span8">
      <span><%= second.title %></span><br>
      <span>by: <%= link_to second.user.username, users_url(second.user.id) %></span>
      <p><%= second.body %></p>
    </div>
  </div>
<% end %>